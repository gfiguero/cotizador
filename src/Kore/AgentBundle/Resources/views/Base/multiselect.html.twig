{% block multiselectcss %}
    <link href="{{ asset('vendor/multiselect/css/bootstrap-multiselect.css') }}" rel="stylesheet">
{% endblock %}

{% block multiselectjs %}
    <script src="{{ asset('vendor/multiselect/js/bootstrap-multiselect.js') }}"></script>
    <script type="text/javascript">
    var optionsMultiselect = {
        maxHeight: 500,
        dropUp: true,
        enableFiltering: true,
        includeSelectAllOption: true,
        enableCaseInsensitiveFiltering: true,
        filterPlaceholder: "Buscar",
        selectAllText: 'Seleccionar todo',
        allSelectedText: 'Todo seleccionado',
        nonSelectedText: 'Ningún elemento seleccionado',
        nSelectedText: 'Elementos seleccionados'
    };
    $('.multiselect').multiselect(optionsMultiselect);
    </script>
{% endblock %}

{% block multiselectjs_issuer %}
    <script src="{{ asset('vendor/multiselect/js/bootstrap-multiselect.js') }}"></script>
    <script type="text/javascript">
    var optionsMultiselect = {
        enableFiltering: true,
        enableCaseInsensitiveFiltering: true,
        filterPlaceholder: "Buscar",
        buttonWidth: '100%',
        maxHeight: 200,
        dropUp: true
    };
    $('.multiselect').multiselect(optionsMultiselect);
    </script>
{% endblock %}