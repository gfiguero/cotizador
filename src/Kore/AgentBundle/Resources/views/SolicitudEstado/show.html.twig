{% extends 'KoreAgentBundle:Base:base.html.twig' %}
{% use 'KoreAgentBundle:Base:navbar.html.twig' %}
{% use 'KoreAgentBundle:Base:sidebar.html.twig' %}
{% use 'KoreAgentBundle:Base:icons.html.twig' %}
{% use 'KoreAgentBundle:Base:flashbag.html.twig' %}

{% trans_default_domain 'KoreAgentBundle' %}

{% block title %}{{ 'solicitudestado.show.title'|trans({'%id%':solicitudEstado.id}) }}{% endblock %}

{% block body %}

    <div id="page-wrapper">
        <div class="row" id="page-header">
            <div class="col-lg-12">
                <h1 class="page-header">{% block icons %}{{ parent() }}{% endblock %}{{ 'solicitudestado.show.title'|trans({'%id%':solicitudEstado.id}) }}</h1>
                {% block flashbag %}{{ parent() }}{% endblock %}
            </div>
        </div>
        <!-- /#page-header -->

        <div class="row" id="page-toolbar">
            <div class="col-lg-12">
                <div class="btn-toolbar">
                    <a type="button" class="btn btn-default" href="{{ path('agent_solicitudestado_index') }}">{{ icon('list') }}&nbsp;{{ 'solicitudestado.index.link'|trans }}</a>
                    <a type="button" class="btn btn-primary" href="{{ path('agent_solicitudestado_edit', { 'id': solicitudEstado.id }) }}">{{ icon('edit') }}&nbsp;{{ 'solicitudestado.edit.link'|trans }}</a>
                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalDelete">{{ icon('trash-o') }}&nbsp;{{ 'solicitudestado.delete.link'|trans }}</button>
                </div>
            </div>
        </div>
        <!-- /#page-toolbar -->

        <div class="row" id="page-show">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h2 class="panel-title">{{ 'solicitudestado.show.data'|trans }}</h2>
                    </div>
                    <div class="panel-body">
                        <dl>
                            <dt>{{ 'solicitudestado.form.id'|trans }}</dt>
                                <dd>{% if solicitudEstado.id %}{{ solicitudEstado.id }}{% endif %}</dd>
                            <dt>{{ 'solicitudestado.form.nombre'|trans }}</dt>
                                <dd>{% if solicitudEstado.nombre %}{{ solicitudEstado.nombre }}{% endif %}</dd>
                            <dt>{{ 'solicitudestado.form.codigo'|trans }}</dt>
                                <dd>{% if solicitudEstado.codigo %}{{ solicitudEstado.codigo }}{% endif %}</dd>
                            <dt>{{ 'solicitudestado.form.descripcion'|trans }}</dt>
                                <dd>{% if solicitudEstado.descripcion %}{{ solicitudEstado.descripcion }}{% endif %}</dd>
                            <dt>{{ 'solicitudestado.form.createdAt'|trans }}</dt>
                                <dd>{% if solicitudEstado.createdAt %}{{ solicitudEstado.createdAt|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' Y - HH:mm") }}{% endif %}</dd>
                            <dt>{{ 'solicitudestado.form.updatedAt'|trans }}</dt>
                                <dd>{% if solicitudEstado.updatedAt %}{{ solicitudEstado.updatedAt|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' Y - HH:mm") }}{% endif %}</dd>                        </dl>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-show -->

        <div id="page-modal">
            <div class="modal fade" id="modalDelete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog modal-md" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">{{ icon('times') }}</button>
                            <h4 class="modal-title" id="myModalLabel">{{ 'solicitudestado.delete.title'|trans }}</h4>
                        </div>
                        <div class="modal-body">
                            <div class="row">
                                <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                <div class="col-xs-10"><p>{{ 'solicitudestado.delete.message'|trans }}</p></div>
                            </div>
                        </div>
                        <div class="modal-footer">
                            {{ form_start(deleteForm) }}
                                <button type="submit" class="btn btn-danger pull-right">{{ icon('trash-o') }}&nbsp;{{ 'solicitudestado.delete.submit'|trans }}</button>
                            {{ form_end(deleteForm) }}
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <!-- /#page-modal -->

    </div>
    <!-- /#page-wrapper -->
{% endblock %}

